machine:
  node:
    version: 6.9.1

dependencies:
  pre:
    - bash ./install.sh

# test:
  # pre:
    # - bash ./install.sh

  # override:
    # - npm run coverage

  # post:
    # - npm run coveralls

general:
  branches:
    ignore:
      - gh-pages
